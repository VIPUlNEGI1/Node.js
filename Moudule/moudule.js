const math = require('buffer')

const math = require('./modilepart');

console.log(math.add(2, 3));  // Output: 5
console.log(math.subtract(5, 2));  // Output: 3


// moudule is th epart of the code or file which are imported be the require keyword
// what is moudule in node

// In Node.js, a module is a reusable block of code whose existence does not impact other code unless explicitly imported. Think of it as a self-contained piece of functionality that you can export and import in different parts of your application.
